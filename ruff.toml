line-length = 120

[lint]
# Ruff 0.16 expanded the default rule set from 59 to 413 rules; pin the pre-0.16
# defaults so that ruff upgrades do not silently change what is linted.
select = ["E4", "E7", "E9", "F"]
